[package]
name = "The_last_Chance"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", features = ["dynamic_linking"] }
bevy_ecs_tilemap = "0.18"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
